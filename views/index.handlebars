     {{!-- * Setup the `index.handlebars` to have the template that Handlebars can render onto.

     * Create a button in `index.handlebars` that will submit the user input into the database. --}}

<h1>Create and devour as many specialty burgers as you can eat at Burger-Aholics</h1>

<h2>Burgers You've Devoured</h2>

<ul>
  {{#each burgers}}
  {{!-- #unless checks for falsey value --}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers to Devour</h2>

<ul>
  {{#each burgers}}
  {{!-- #if checks an if condition for boolean or if something exists --}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<li>
	{{name}}
</li>

<h2>Make Your Own Burger:</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <input type="input" value="0"> 
  </div>

  <button type="submit">Add to List</button>
</form>