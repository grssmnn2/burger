 {{!-- * Setup the `index.handlebars` to have the template that Handlebars can render onto. * Create a button in `index.handlebars`
that will submit the user input into the database. --}}

<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <h1>Create and devour as many specialty burgers as you can eat at Burger-Aholics</h1>
    
    </div>
    <div class="col-sm-4">
      <h2>Burgers You've Devoured</h2>
      <ul>
        {{#each burgers}}
        {{#unless devoured}} 
        {{burger_name}}
        {{/unless}} {{/each}}
      </ul>

        <li>          
        {{burger_name}}      
       </li>
    </div>
    <div class="col-sm-4">
        <img src="/assets/img/tasty.jpg" alt="hamburger">
    </div>
    <div class="col-sm-4">
      <h2>Burgers to Devour</h2>

      <ul>
        {{#each burgers}}
        {{#if devoured}} 
        <p>{{burger_name}}</p>
         <button class="btn change-devour" type="submit">DEVOUR</button>
         {{/if}}
         {{/each}}
      </ul>

      <li>
        {{burger_name}}
      </li>
    </div>
  </div>
</div>
<h2>Make Your Own Burger:</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <button class="create-form submit" type="submit">Add to List</button>
</form>